apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "pdb-operator.fullname" . }}-rolebinding
  labels:
    {{- include "pdb-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "pdb-operator.fullname" . }}-role
subjects:
  - kind: ServiceAccount
    name: {{ include "pdb-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
